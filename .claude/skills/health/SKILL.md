---
name: health
description: 프로젝트 건강 상태 원클릭 체크. 빌드, 타입체크, 문서 일치율, 기술 부채 종합 점검. "건강", "health", "체크", "점검", "상태 확인" 키워드에 반응.
---

# Health - 프로젝트 건강 상태 체크 스킬

## 역할

프로젝트의 전반적인 건강 상태를 빠르게 점검하고 문제를 조기에 발견합니다.

**핵심 가치:**
- 원클릭으로 전체 상태 파악
- 빌드/타입/린트 오류 즉시 발견
- 문서↔코드 일치율 확인
- 기술 부채 현황 파악

## 사용법

| 명령 | 설명 |
|------|------|
| `/health` | 전체 건강 상태 체크 |
| `/health quick` | 빠른 체크 (빌드+타입만) |
| `/health build` | 빌드 상태만 |
| `/health docs` | 문서 일치율만 |
| `/health debt` | 기술 부채만 |

## 체크 항목

### 1. 빌드 상태 (Build)

#### Backend
```bash
cd backend && ./gradlew clean build -x test
```
- ✅ 성공: 빌드 통과
- ❌ 실패: 컴파일 오류 목록 표시

#### Frontend
```bash
cd frontend && npm run build
```
- ✅ 성공: 빌드 통과
- ❌ 실패: 빌드 오류 목록 표시

### 2. 타입 체크 (Type)

#### Frontend TypeScript
```bash
cd frontend && npx vue-tsc --noEmit
```
- ✅ 성공: 타입 오류 없음
- ⚠️ 경고: 타입 오류 N개 (목록 표시)

### 3. 문서 일치율 (Docs)

#### 체크 항목
| 비교 대상 | 계산 방법 |
|----------|---------|
| Controller vs API 문서 | Controller 수 / 문서화된 API 수 |
| Service vs 개념 문서 | Service 수 / 문서화된 Service 수 |
| 대형 파일 문서화 | 1000줄+ 파일 / 문서화된 파일 |

#### 판정 기준
- ✅ 80%+ : 양호
- ⚠️ 60-79% : 주의 필요
- ❌ 60% 미만 : 즉시 조치 필요

### 4. 기술 부채 (Debt)

#### 체크 항목
| 항목 | 기준 | 심각도 |
|------|------|--------|
| 대형 파일 | 1000줄+ | 🟠 Medium |
| 초대형 파일 | 3000줄+ | 🔴 High |
| TODO 주석 | TODO/FIXME 개수 | 🟡 Low |
| Deprecated 사용 | @Deprecated 개수 | 🟠 Medium |
| console.log 잔재 | FE 로그 개수 | 🟡 Low |

### 5. 진행 중 작업 (Progress)

```bash
# 진행 중 작업
ls docs/work/active/
```

## 출력 형식

### 전체 체크 (`/health`)

```
🏥 ACS 프로젝트 건강 상태
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📅 체크일: 2026-01-07 14:30

┌─────────────────────────────┐
│  종합 점수: 78/100 (양호)   │
└─────────────────────────────┘

📦 빌드 상태
├── Backend:  ✅ 성공
└── Frontend: ✅ 성공

🔷 타입 체크
└── TypeScript: ⚠️ 3개 오류
    └── src/stores/icdStore.ts:142 - Type 'undefined' is not assignable
    └── src/pages/mode/PassSchedulePage.vue:89 - Property 'x' does not exist

📚 문서 일치율
├── API 문서:      9/9 (100%) ✅
├── Service 문서:  8/13 (62%) ⚠️
└── 대형 파일:     2/5 (40%) ❌

⚠️ 기술 부채
├── 🔴 High:   2개 (초대형 파일)
├── 🟠 Medium: 5개 (대형 파일, Deprecated)
└── 🟡 Low:    12개 (TODO, console.log)

🚧 진행 중 작업
├── Features: 4개
│   ├── Architecture_Refactoring
│   ├── PassSchedule_Data_Structure_Refactoring
│   ├── PassSchedule_Keyhole_Display_Enhancement
│   └── PassSchedule_Workflow
└── Bugfixes: 1개
    └── PassSchedule_ApplyRowColors_Infinite_Loop

💡 권장 조치
1. [즉시] TypeScript 오류 3개 수정
2. [이번 주] Service 문서화 5개 추가
3. [이번 달] EphemerisService.kt 분리 (4,986줄)
```

### 빠른 체크 (`/health quick`)

```
🏥 ACS 빠른 체크 (2026-01-07)

Backend Build:  ✅
Frontend Build: ✅
TypeScript:     ⚠️ 3 errors

상세: /health 실행
```

### JSON 형식 (`/health json`)

```json
{
  "date": "2026-01-07T14:30:00",
  "score": 78,
  "build": {
    "backend": "success",
    "frontend": "success"
  },
  "typeCheck": {
    "status": "warning",
    "errors": 3
  },
  "docs": {
    "api": { "current": 9, "total": 9, "rate": 100 },
    "service": { "current": 8, "total": 13, "rate": 62 },
    "largeFiles": { "current": 2, "total": 5, "rate": 40 }
  },
  "debt": {
    "high": 2,
    "medium": 5,
    "low": 12
  },
  "progress": {
    "features": 4,
    "bugfixes": 1
  }
}
```

## 점수 계산

| 항목 | 비중 | 감점 기준 |
|------|------|----------|
| 빌드 성공 | 30점 | 실패 시 0점 |
| 타입 체크 | 20점 | 오류당 -2점 |
| 문서 일치율 | 25점 | (일치율% × 0.25) |
| 기술 부채 | 15점 | High -5, Medium -2, Low -1 |
| 진행 관리 | 10점 | 문서 있으면 만점 |

### 등급
| 점수 | 등급 | 상태 |
|------|------|------|
| 90+ | A | 🟢 우수 |
| 80-89 | B | 🟢 양호 |
| 70-79 | C | 🟡 보통 |
| 60-69 | D | 🟠 주의 |
| 60 미만 | F | 🔴 위험 |

## 실행 스크립트

### 빌드 체크
```bash
# Backend
cd backend && ./gradlew clean build -x test 2>&1 | tail -20

# Frontend
cd frontend && npm run build 2>&1 | tail -20
```

### 타입 체크
```bash
cd frontend && npx vue-tsc --noEmit 2>&1
```

### 문서 일치율
```bash
# Controller 수
find backend/src -path "*controller*" -name "*.kt" ! -name "*Test*" | wc -l

# Service 수
find backend/src -path "*service*" -name "*Service.kt" ! -name "*Test*" | wc -l

# 문서 수
find docs -name "*.md" | wc -l

# 대형 파일 수 (1000줄+)
find backend/src frontend/src -name "*.kt" -o -name "*.vue" -o -name "*.ts" | \
  xargs wc -l 2>/dev/null | awk '$1 >= 1000 {print}' | wc -l
```

### 기술 부채
```bash
# TODO/FIXME
grep -r "TODO\|FIXME" backend/src frontend/src --include="*.kt" --include="*.vue" --include="*.ts" | wc -l

# Deprecated
grep -r "@Deprecated" backend/src --include="*.kt" | wc -l

# console.log
grep -r "console\.log" frontend/src --include="*.vue" --include="*.ts" | wc -l
```

## 연계 스킬/에이전트

| 문제 발견 시 | 연계 |
|-------------|------|
| 빌드 실패 | `debugger` 에이전트 |
| 타입 오류 | `fullstack-helper` 에이전트 |
| 문서 부족 | `/docs` 스킬, `doc-syncer` 에이전트 |
| 기술 부채 | `refactorer`, `performance-analyzer` 에이전트 |
| 상세 현황 | `/status` 스킬 |

## 권장 실행 주기

| 시점 | 명령 | 목적 |
|------|------|------|
| 작업 시작 전 | `/health quick` | 기본 상태 확인 |
| 작업 완료 후 | `/health` | 전체 상태 확인 |
| 주간 | `/health` + 저장 | 추이 확인 |

## 트러블슈팅

### Q: 빌드가 계속 실패해요
A: `./gradlew clean`으로 캐시 정리 후 재시도

### Q: 타입 오류가 너무 많아요
A: `// @ts-ignore` 임시 사용 후 점진적 수정

### Q: 문서 일치율 계산이 이상해요
A: 문서 위치가 규칙대로인지 확인 (references/, concepts/)

---

**스킬 버전:** 1.0.0
**작성일:** 2026-01-07
**호환:** ACS 프로젝트 전용
